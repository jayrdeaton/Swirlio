import { defaultColors, SeedColor } from '@rific/auto-paper'

export const NEUTRAL_AND_VIVID_COLORS: SeedColor[] = [{ label: 'Black', value: '#000000' }, { label: 'White', value: '#FFFFFF' }, ...defaultColors]
